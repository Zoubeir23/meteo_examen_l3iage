import 'package:flutter/material.dart';

/// Shared color palette for the light and dark themes.
class AppColors {
  const AppColors._();

  static const Color skyBlue = Color(0xFF3D7EFF);
  static const Color skyBlueDark = Color(0xFF5B93FF);
  static const Color sunAmber = Color(0xFFFFA940);

  static const Color lightBackground = Color(0xFFF5F8FD);
  static const Color lightSurface = Color(0xFFFFFFFF);

  static const Color darkBackground = Color(0xFF0E1420);
  static const Color darkSurface = Color(0xFF161E2E);

  static const Color error = Color(0xFFE5484D);
}
